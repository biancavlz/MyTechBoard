{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "env": {
                "NODE_ENV": "development"
            },
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/bin/www"
        }
    ]
}
